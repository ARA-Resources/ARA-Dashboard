version: '3.8'

services:
  ara-dashboard:
    build: .
    container_name: ara-dashboard
    restart: unless-stopped
    ports:
      - "3001:3000"
    volumes:
      - ara_pgdata:/var/lib/postgresql/data
    env_file:
      - .env

volumes:
  ara_pgdata:
