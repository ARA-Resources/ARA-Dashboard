version: '3.8'

services:
  ara-dashboard:
    build: .
    container_name: ara-dashboard-prod
    restart: unless-stopped
    ports:
      - "3000:3000"   # Frontend, Backend
      - "5433:5432"   # host:container — Database
    volumes:
      - ara_pgdata:/var/lib/postgresql/data
      - ara_appdata:/app/.data
    env_file:
      - .env

volumes:
  ara_pgdata:
  ara_appdata:
