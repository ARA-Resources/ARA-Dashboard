import type { MultiSchedulerStatus } from "@/types/dataset-schedule";

/** @deprecated Prefer MultiSchedulerStatus — kept for existing imports */
export type DatasetSchedulerStatus = MultiSchedulerStatus;
