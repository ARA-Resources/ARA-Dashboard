"use client";

/**
 * Executive P-Dashboard filter chrome.
 * Reuses the shared openings filter panel controls with Executive-only schema fields
 * (Team Member 1, Priority, Job Status, Posted) supplied by the P-Dashboard API.
 */
export { OpeningsFilterPanel as ExecutivePDashboardFilters } from "@/components/dashboard/accenture/openings-filter-panel";
